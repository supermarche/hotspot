import java.text.SimpleDateFormat
import java.time.format.DateTimeFormatter

object Konstanten {
    val DF_ISO = DateTimeFormatter.ofPattern("YYYY-MM-DD")
    val DF = DateTimeFormatter.ofPattern("dd.MM.yyyy")
}